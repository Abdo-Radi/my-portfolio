# Dependencies & build output (recreated inside the image)
node_modules
.next
out
build
coverage

# Version control & CI
.git
.gitignore
.github

# Env files — never copy secrets into the build context / image
.env*

# Docker & local tooling
Dockerfile
.dockerignore
docker-compose.yml
*.local
.vscode
.idea

# Docs & logs
README.md
npm-debug.log*
